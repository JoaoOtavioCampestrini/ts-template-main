import Scanner from "@codeea/scanner";

async function main() {
  const scanner = new Scannerner();
  const menu = `
    0 - SAIR
    1 - SOMA
    2 - SUBTRACAO
    3 - DIVISAO
    4 - MULTIPLICACAO
    5 - POTENCIACAO
    6 - CHECA SE NUMERO É PAR
  `;

  let operacao = 0;

  do {
switch (key) {
  case value:
    
    break;

  default:
    break;
}
  } while (operacao !== 0);
}

main ();

